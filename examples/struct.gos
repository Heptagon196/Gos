#!/usr/bin/env gos
struct (Box) {
    length = 0;
    breadth = 0;
    height = 0;
    read = func() {
        read(length, breadth, height);
    }
    getVolume = func() {
        return(length * breadth * height);
    }
}
x := Box;
x.read();
println(x.getVolume());
