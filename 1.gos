import "std"
class Main {
    var id: string = "hello";
    func isNumType(obj: object) bool {
        return obj.type == "int" || obj.type == "double";
    }
    func main(args: string) int {
        var i: int = 0;
        i = 12;
        print(i);
        var f = lambda(a: int, b: int) bool {
            return a > b;
        };
        for (var i: int = 0; i < 10; i++) {
        }
        foreach (var i: args) {
        }
        return 0;
    }
}
