#!/usr/bin/env gos
struct (Box) {
    length = 0;
    breadth = 0;
    height = 0;
    read = func() {
        read(this.length, this.breadth, this.height);
    }
    getVolume = func() {
        return(this.length * this.breadth * this.height);
    }
}
x := Box;
x.read();
println(x.getVolume());
